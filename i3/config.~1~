# It will not be overwritten, so edit it as you like.
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!
exec --no-startup-id nitrogen --restore
set $mod Mod4
#set $mod Mod2
# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:Fira Code 1
default_border pixel 3
#default_floating_border pixel 1

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:Hack Nerd Font 10
font pango:DejaVu Sans Mono 10

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex --autostart --environment i3

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet
# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
#bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status
bindsym $mod+Shift+F8 exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && $refresh_i3status
bindsym $mod+Shift+F7 exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && $refresh_i3status
bindsym $mod+Shift+F9 exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# move tiling windows via drag & drop by left-clicking into the title bar,
# or left-clicking anywhere into the window while holding the floating modifier.
tiling_drag modifier titlebar

# start a terminal
bindsym $mod+Return exec st -f "JetBrains MonoNL Nerd Font:style=Light:size=12.5" 
bindsym $mod+ctrl+return exec kitty
#bindsym $mod+Return exec xfce4-terminal  
#bindsym $mod+Return exec st -f "JetBrains MonoNLNerd Font:style=Light:size=13.5" 

#bindsym $mod+Return exec xfce4-terminal

#bindsym $mod+Return exec st
#bindsym $mod+Return exec alacritty
# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
#bindsym $mod+d exec --no-startup-id dmenu_run
# A more modern dmenu replacement is rofi:
# bindcode $mod+40 exec "rofi -modi drun,run -show drun"
# There also is i3-dmenu-desktop which only displays applications shipping a
# .desktop file. It is a wrapper around dmenu, so you need that installed.
# bindcode $mod+40 exec --no-startup-id i3-dmenu-desktop

# change focus

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
#bindsym $mod+Shift+j move left
#bindsym $mod+Shift+k move down
#bindsym $mod+Shift+l move up
#bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h
#bindsym $mod+Alt+h focus right, split h
# split in vertical orientation
bindsym $mod+v split v
# enter fullscreen mode for the focused container
bindsym $mod+F11 fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
#bindsym $mod+s layout stacking
bindsym $mod+Shift+x layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10
bindysm $mod+x bash ~/what.sh
# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
       bindsym Mod4+Shift+h resize shrink width 1 px or 1 ppt
       bindsym Mod4+Shift+k resize grow height 1 px or 1 ppt
       bindsym Mod4+shift+j resize shrink height 1 px or 1 ppt
       bindsym Mod4+Shift+l resize grow width 1 px or 1 ppt

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)

## COLOR SCHEMES
#6272A4

#DRACULA_THEME
# class                 border  bground text    indicator child_border
#client.focused          #b392f9 #bf39f9 #282a36 #bd92f9   #bd93f9
#client.focused_inactive #44475A #44475A #F8F8F2 #44475A   #44475A
#client.unfocused        #282A36 #282A36 #BFBFBF #282A36   #282A36
#client.urgent           #44475A #FF5555 #F8F8F2 #FF5555   #FF5555
#client.placeholder      #282A36 #282A36 #F8F8F2 #282A36   #282A36




# DRACULA#bd93f9
##### #6272A4 #6272A4 #F8F8F2 #6272A4   #6272A4

# class                 border  bground text    indicator child_border
client.focused          #b392f9 #bf39f9 #282a36 #bd92f9   #bd93f9
client.focused_inactive #ffffff #6272a4 #f8f8f2 #6272a4   #6272a4
client.unfocused        #b392f9 #282A36 #BFBFBF #282A36   #282A36
client.urgent           #44475A #FF5555 #F8F8F2 #FF5555   #FF5555
client.placeholder      #282A36 #282A36 #F8F8F2 #282A36   #282A36
#Hello This Shit is GAS 
#client.background       #F8F8F
smart_gaps on
for_window [floating] move position center, resize set 1280x768
gaps outer 3
gaps inner 8
#gaps outer 5
#gaps inner 5 
#gaps outer 2
#gaps inner 4
title_align center
exec --no-startup-id autotiling
exec searxng-run
exec_always ~/.config/polybar/launch.sh
exec autotiling-rs
exec --no-startup-id valent --gapplication-service 
exec mpd
exec mpd-mpris
exec --no-startup-id dunst
exec --no-startup-id xsetroot -cursor_name left_ptr
#exec redshift -l 22:72
exec xrdb -merge ~/.Xresources
####### Web Browser Bindings #######
bindsym $mod+f exec helium-browser --profile-directory="Default"
bindsym $mod+Shift+w exec helium-browser --profile-directory="Profile 2"
bindsym $mod+ctrl+c exec bash ~/firefox.sh
bindsym $mod+Shift+b exec mercury-browser-sse4
bindsym $mod+Shift+f exec helium-browser --profile-directory="Profile 1" 
bindsym $mod+ctrl+m exec chromium --incognito $(xsel -bo)

####### Regular App Binding #######
bindsym $mod+n exec spacefm
bindsym $mod+shift+n exec nitrogen
bindsym $mod+k exec keepassxc
bindsym $mod+p exec pavucontrol
bindsym $mod+q exec qbittorrent
bindsym $mod+g exec galculator
bindsym $mod+Shift+d exec discord
bindsym $mod+s exec SoulSeekQT
bindsym $mod+l exec vlc
bindsym $mod+t exec telegram-desktop
bindsym $mod+shift+t exec org.telegram.desktop 
bindsym $mod+m exec java -jar ~/Desktop/Minecraft/TLauncher-2.899.jar

assign [class="Audacity"] 4

####### TUI/CLI app Binding #######
bindsym $mod+r exec dmenu_run -c -l 20 -fn "JetBrains MonoNL Nerd Font:style=Light:size=13"
#t/Shield.sh#bindsym $mod+r exec rofi  -show drun 
#bindsym $mod+shift+e exec bash ~/.config/i3/dmenu_exit.sh
bindsym $mod+shift+p exec bash ~/.youtube.sh
bindsym $mod+w exec bash ~/.config/script/waldl
bindsym $mod+c exec st -f "JetBrains MonoNL NerdFont:style=Light:size=12.5" -e ncmpcpp 
bindsym $mod+ctrl+g exec  st -f "JetBrains MonoNL NerdFont:style=Light:size=12.5" -e sudo auto-cpufreq --force powersave
bindsym $mod+ctrl+h exec  st -f "JetBrains MonoNL NerdFont:style=Light:size=12.5" -e sudo auto-cpufreq --force performance
#bindsym $mod+ctrl+q exec st -f "JetBrains MonoNL NerdFont:style=Light:size=12.5" -e bash ~/qobuz.sh
bindsym $mod+b exec qrencode -t PNG -s 22 --background=282a36 --foreground=bd93f9 -o /tmp/file.png "$(xsel -bo)" && sxiv /tmp/file.png 
bindsym $mod+Shift+y exec bash ~/.config/script/Youtube-local.sh 
bindsym $mod+ctrl+y exec bash ~/yt-dlp.sh $(xsel -bo)
#bindsym $mod+b exec st -f "JetBrains MonoNL NerdFont:style=Light:size=13" -e newsboat
bindsym $mod+shift+a exec bash ~/.mpv-mp3.sh
bindsym $mod+i exec bash ~/.config/script/feh.sh
bindsym $mod+Shift+i exec wget $(xsel -bo)
bindsym $mod+Shift+Return exec kitty 
bindsym $mod+d exec st -f "JetBrains MonoNL NerdFont:style=Light:size=12" -e bash ~/test.sh 
#bindsym $mod+x [floating] resize set 900 500 ; move position center
bindsym $mod+x [floating] resize set 900 500 ; move position center 
bindsym $mod+j [floating] resize set 500 200 ; move position center
bindsym $mod+z [floating] resize set 500 300 ; move position 100 0
bindsym $mod+ctrl+Right exec i3move right
bindsym $mod+ctrl+Left exec i3move left
bindsym $mod+ctrl+Up exec i3move up
bindsym $mod+ctrl+Down exec i3move down
bindsym $mod+ctrl+t exec bash ~/.config/script/wall.sh 
bindsym $mod+o focus mode_toggle
bindsym $mod+Shift+v exec st -f "JetBrains MonoNL NerdFont:style=Light:size=12.5" -e nvim ~/Documents/notes.txt
####### Assorted App Binding #######
bindsym $mod+ctrl+j java -jar ~/Desktop/Tlauncher/TLauncher-2.885.jar
bindsym $mod+shift+m exec i3-sensible-terminal -e "sudo mount /dev/sda1 /home/aditya/4TB" 
bindsym Print exec flameshot full
bindsym $mod+Print exec flameshot gui

####### Volume Bindings #######
#bindsym $Mod+Shift+F7 exec --no-startup-id pactl set-sink-volume 0  -5% #increase sound volume
#bindsym $Mod+Shift+F8 exec --no-startup-id pactl set-sink-volume 0  +5% #increase sound volume
#bindsym $Mod+Shift+F9 exec --no-startup-id pactl set-sink-mute 0 toggle # mute sound
#bindsym $Mod+Shift+F8 exec pamixer -i 5
#bindsym $Mod+Shift+F7 exec pamixer -d 5
#bindysm $Mod+Shift+F9 exec pamixer -t
#bindsym $Mod+Shift+F7 exec --no-startup-id pactl set-sink-volume 0 -5% #decrease sound volume
bindsym $mod+Shift+u exec playerctl --player=mpd play-pause 
bindsym $mod+ctrl+n exec playerctl --player=mpd next 
bindsym $mod+ctrl+p exec playerctl --player=mpd previous
###### Youtube Binding ######
bindsym $mod+y exec bash ~/.config/script/720.sh
bindsym $mod+u exec bash ~/.config/script/1080.sh
bindsym $mod+shift+g exec bash ~/.config/script/Shield.sh
###### Brightness Control ######
bindsym $mod+ctrl+1 exec brightnessctl set 10%
bindsym $mod+ctrl+2 exec brightnessctl set 20%
bindsym $mod+ctrl+3 exec brightnessctl set 30%
bindsym $mod+ctrl+4 exec brightnessctl set 40%
bindsym $mod+ctrl+5 exec brightnessctl set 50%
bindsym $mod+ctrl+6 exec brightnessctl set 60%
bindsym $mod+ctrl+7 exec brightnessctl set 70%
bindsym $mod+ctrl+8 exec brightnessctl set 80%
bindsym $mod+ctrl+9 exec brightnessctl set 90%
bindsym $mod+ctrl+0 exec brightnessctl set 100%








